import { FolderTree, Plus } from "lucide-react";
import { saveCategory } from "@/app/(dashboard)/admin/categorias/actions";
import { requireAdmin } from "@/lib/auth";
import { createSupabaseServerClient } from "@/lib/supabase/server";
const input="h-10 w-full rounded-xl border border-slate-200 px-3 text-sm";
export default async function CategoriesPage(){await requireAdmin("products.view");const {data}=await (await createSupabaseServerClient())!.from("categories").select("*").order("sort_order");return <div className="mx-auto max-w-5xl"><div className="mb-7"><p className="text-sm text-slate-500">Organização do catálogo</p><h1 className="font-display mt-1 text-3xl font-black uppercase text-[#001b3b]">Categorias</h1></div><div className="grid items-start gap-5 lg:grid-cols-[1fr_320px]"><section className="space-y-3">{data?.map(category=><form action={saveCategory} key={category.id} className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 sm:grid-cols-[1fr_1fr_90px_auto]"><input type="hidden" name="id" value={category.id}/><label><span className="mb-1 block text-[0.65rem] font-bold uppercase text-slate-400">Nome</span><input name="name" defaultValue={category.name} className={input}/></label><label><span className="mb-1 block text-[0.65rem] font-bold uppercase text-slate-400">Slug</span><input name="slug" defaultValue={category.slug} className={input}/></label><label><span className="mb-1 block text-[0.65rem] font-bold uppercase text-slate-400">Ordem</span><input name="sort_order" type="number" defaultValue={category.sort_order} className={input}/></label><div className="flex items-end gap-3 pb-2"><label className="text-xs"><input type="checkbox" name="active" defaultChecked={category.active} className="mr-1 accent-sky-500"/> Ativa</label><label className="text-xs"><input type="checkbox" name="featured" defaultChecked={category.featured} className="mr-1 accent-sky-500"/> Home</label></div><label className="sm:col-span-3"><span className="mb-1 block text-[0.65rem] font-bold uppercase text-slate-400">Descrição</span><input name="description" defaultValue={category.description??""} className={input}/></label><button className="h-10 self-end rounded-xl bg-slate-900 px-4 text-xs font-bold text-white">Salvar</button></form>)}</section><form action={saveCategory} className="rounded-2xl border border-slate-200 bg-white p-5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-sky-50 text-sky-500"><Plus/></span><h2 className="mt-4 font-bold">Nova categoria</h2><div className="mt-4 space-y-3"><input name="name" required placeholder="Nome" className={input}/><input name="slug" required placeholder="slug-amigavel" className={input}/><input name="description" placeholder="Descrição" className={input}/><input name="sort_order" type="number" defaultValue={0} className={input}/><label className="block text-xs"><input type="checkbox" name="active" defaultChecked className="mr-2 accent-sky-500"/>Categoria ativa</label><label className="block text-xs"><input type="checkbox" name="featured" className="mr-2 accent-sky-500"/>Destacar na home</label><button className="h-11 w-full rounded-xl bg-sky-500 text-xs font-black text-white"><FolderTree size={15} className="mr-2 inline"/>Criar categoria</button></div></form></div></div>}

